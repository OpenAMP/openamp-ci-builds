header:
  version: 9
  includes:
    - openamp.yml

machine: qemuarm64

local_conf_header:
  bugs: |
    # Only ping until errors can be resolved
    TEST_SUITES = "ping"

target:
  - openamp-image-minimal
