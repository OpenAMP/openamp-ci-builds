header:
  version: 9
  includes:
    - st-oss.yml

machine: stm32mp15-disco-oss

local_conf_header:
  bugs: |
    # Only ping until errors can be resolved
    TEST_SUITES = "ping"

target:
  - core-image-base
